# AutoDrama 开发步骤清单

> 基于《界面设计备忘录》+《技术架构文档》拆分  
> 每个步骤是 Kimi Code 可独立完成的任务单元  
> **最后更新**：2026-05-22（已整合备忘录更新 + 批量生成重构 + 商业化前置埋点）

---

## MVP1（先实现）

### 第一阶段：数据库底层

#### ✅ M1-01 数据库迁移——projects/shots/characters 新增字段
- **做什么**：给已有表加字段，不 DROP 重建
  - `projects`：新增 `era`, `negative_prompt`, `model_config_json`, `parent_project_id`
  - `shots`：新增 `dialogue`, `first_frame_image_path`, `last_frame_image_path`, `video_path`, `voice_path`
  - `characters`：新增 `skin_images`
- **依赖**：无
- **验收标准**：`npm run typecheck` 通过；启动应用不报错；旧项目数据不丢失

#### ✅ M1-02 新建核心表——props / shot_props / generation_tasks / prompt_templates
- **做什么**：
  - 创建 `props` 表（同 scenes 结构）
  - 创建 `shot_props` 关联表
  - 创建 `generation_tasks` 任务队列表（含 `channel` 字段）
  - 创建 `prompt_templates` 模板表
  - `initDatabase()` 中插入官方预设模板（标准分镜/精细镜头拆分/纯分镜/短视频制作）
- **依赖**：M1-01
- **验收标准**：数据库初始化成功；`db.prepare('SELECT * FROM prompt_templates').all()` 返回4条官方模板

#### ✅ M1-03 新建资产历史记录表
- **做什么**：
  - `character_images` / `scene_images` / `prop_images` / `shot_images` / `shot_videos`
  - 每张表包含 `is_selected` / `has_new_badge`（shot_videos 专属）字段
- **依赖**：M1-02
- **验收标准**：5张表创建成功；`getDb()` 可正常访问

---

### 第二阶段：服务层扩展

#### ✅ M1-04 项目 CRUD 扩展
- **做什么**：
  - `createProject` 支持 `era`, `negative_prompt`, `model_config_json`, `parent_project_id`
  - `getProject` 返回新字段
  - `updateProject` 支持修改风格/比例/年代/模型配置
  - 续集项目创建逻辑：复制父项目的角色/场景/道具数据
- **依赖**：M1-01
- **验收标准**：创建项目时 `model_config_json` 有默认值；getProject 能读到新字段

#### ✅ M1-05 资产服务层——characters / scenes / props 的 CRUD
- **做什么**：
  - `createCharacter` / `updateCharacter` / `deleteCharacter` / `getCharactersByProject`
  - `createScene` / `updateScene` / `deleteScene` / `getScenesByProject`
  - `createProp` / `updateProp` / `deleteProp` / `getPropsByProject`
  - 名称在项目内唯一性校验
- **依赖**：M1-02
- **验收标准**：IPC 调用 `window.api.createCharacter` 成功；删除角色时关联的 `shot_characters` 级联清理

#### ✅ M1-06 分镜查询服务层
- **做什么**：
  - `getShotsByChapter` 返回完整分镜数据
  - `getShotWithAssociations`：JOIN 查询返回分镜 + 关联角色/场景/道具
  - `loadExistingData` 优化版：用批量 JOIN 查询替代 N+1
  - 支持分镜上下移动（调整 `shot_index`）
  - 支持分镜删除（级联清理关联表）
- **依赖**：M1-05
- **验收标准**：`window.api.getShotsWithAssoc(chapterId)` 返回包含角色/场景/道具数组的分镜数据

#### ✅ M1-07 提示词模板服务
- **做什么**：
  - `getPromptTemplates(category)` 按分类查询模板
  - `savePromptTemplate` 保存自定义模板
  - `deletePromptTemplate` 删除自定义模板（官方模板不可删）
- **依赖**：M1-02
- **验收标准**：AI解析弹窗的下拉能读到4个官方模板；保存自定义模板后出现在列表中

#### ✅ M1-08 AI 解析剧本接口升级
- **做什么**：
  - `autoProcess` 支持 `promptTemplate` 参数（传入模板内容）
  - 解析结果包含 `props`（道具）
  - 实现「只创建、不覆盖」原则：已存在的角色/场景/道具只建关联不修改
  - 支持 `era` / `aspectRatio` / `model` 参数
  - 保存 `script_text` 到 projects 表
- **依赖**：M1-05, M1-07
- **验收标准**：同一项目二次解析不覆盖已有角色的 description；新剧本新增的角色正常创建

---

### 第二阶段半：Home.vue 改造

#### ✅ M1-09 Home.vue 改造——项目列表 + 创建入口
- **做什么**：
  - 改造现有 Home.vue：上方保留项目列表卡片网格，下方/侧边增加"创建项目"入口
  - 创建项目弹窗：项目名称输入框 + 项目目录输入框（默认`userData/projects`，可修改，带文件夹选择按钮）+ 取消/创建按钮
  - ~~画面风格/画面比例~~ 从创建弹窗彻底移除（已挪到AI解析弹窗）
  - 支持"基于已有项目创建"入口（MVP1占位，弹窗内加一个下拉选择已有项目）
  - 点击项目卡片进入编辑器
- **依赖**：M1-04
- **验收标准**：能看到已有项目卡片；点击创建弹出表单；创建后新项目出现在列表中；目录可修改

---

### 第三阶段：项目总览页 + AI解析弹窗

#### ✅ M1-09b 项目总览页改造
- **做什么**：
  - 移除原有的"剧本输入区"
  - 新增：画面风格选择网格（13种预设 + 自定义占位）
  - 新增：画面比例选择（16:9 / 9:16 / 1:1）
  - 新增：AI解析剧本按钮（打开弹窗）
  - 新增：数据统计卡片（角色数/场景数/道具数/剧集数/分镜数）——有数据时显示，无数据时隐藏
  - 风格/比例选择后 `UPDATE projects` 保存
- **依赖**：M1-09
- **验收标准**：选择风格后刷新页面，选择状态保留；点击"AI解析剧本"弹出弹窗

#### ✅ M1-10 AI解析弹窗
- **做什么**：
  - 弹窗布局：标题 + Tab切换（AI生成/手动切分占位）+ 关闭按钮
  - 剧本输入区：textarea + 字数统计（建议500字以上）
  - 画面风格选择网格（同总览页）
  - 画面比例选择
  - 提示词模板下拉（读 `prompt_templates`，可展开预览内容）
  - 模型选择下拉（读当前配置的语言模型）
  - 底部按钮：暂时跳过 + 一键生成分镜（loading状态）
  - 解析完成后自动跳转到剧集结构页
- **依赖**：M1-07, M1-08, M1-09
- **验收标准**：粘贴剧本后点击生成，进度推送正常，完成后自动跳转，数据库有 chapters/shots/characters/scenes/props 数据

---

### 第四阶段：剧集结构页骨架

#### ✅ M1-11 剧集结构页布局骨架
- **做什么**：
  - 左侧导航栏更新：取消角色/场景/道具独立页，只剩"项目总览"和"剧集结构"
  - 剧集结构页整体布局：顶部工具栏 + 中间横向列表区 + 右侧面板
  - 横向列表：固定列头 + 可滚动内容区
  - 列头双行设计：上行列名称，下行批量按钮占位
  - 序号列：分镜编号 + 复选框（列头全选框）
- **依赖**：M1-06
- **验收标准**：进入剧集结构页能看到列头和序号列，列头有"批量生成"占位按钮

#### ✅ M1-12 分镜列表——剧本/出场人物/场景/道具列
- **做什么**：
  - 剧本列：显示 `description`（截断+换行），双击进入编辑，下方显示关联标签（角色/场景/道具名标签）
  - 出场人物列：固定列宽，一行3个缩略图（灰底+名字占位），点击切换右侧面板
  - 场景列：同出场人物列逻辑
  - 道具列：同出场人物列逻辑
  - 智能关联：编辑时输入未关联的名称，自动建立关联（创建 `shot_characters` / `shot_scenes` / `shot_props` 记录）
- **依赖**：M1-11
- **验收标准**：能看到分镜列表和角色/场景/道具缩略图占位；双击剧本列可编辑文本；输入角色名后自动关联

#### ✅ M1-13 分镜列表——首帧提示词/尾帧提示词/首帧/尾帧/视频/操作列
- **做什么**：
  - 首帧提示词列：显示 `first_frame_prompt`，双击编辑，下方关联标签+高亮
  - 尾帧提示词列：同上
  - 首帧列：灰底占位 + "首帧"文字，点击打开右侧面板
  - 尾帧列：灰底占位 + "尾帧"文字
  - 视频列：灰底占位 + "视频"文字
  - 操作列：上下移动按钮 + 删除按钮
  - 配音列：按钮占位，点击右侧面板显示"功能开发中"
- **依赖**：M1-12
- **验收标准**：所有列可见；上下移动能调整分镜顺序；删除能移除分镜

#### ✅ M1-14 右侧面板——常驻状态（角色/场景/道具Tab）
- **做什么**：
  - 三个Tab：角色/场景/道具，右侧"批量生成"按钮 + 搜索框
  - 内容区分"作品中(N/M)"和"全部可用(N/M)"
  - "全部可用"逻辑：在分镜提示词中被高亮关联的 = 可用；零高亮 = 不可用
  - 每个资产是缩略图卡片（灰底+名字），点击进入详情
  - 底部"创建"按钮 + "从其他项目导入"按钮（MVP1占位）
- **依赖**：M1-12
- **验收标准**：Tab切换正常；"作品中"显示当前项目的资产；"全部可用"按高亮逻辑分组

#### ✅ M1-15 右侧面板——详情状态（角色/场景/道具详情）
- **做什么**：
  - 返回按钮回到常驻状态
  - 大图预览区（灰底占位）+ 全屏占位
  - 角色名称文本框（**可编辑**，改名触发全局联动）
  - 角色描述文本框（较大，**可编辑**）
  - 选择图片按钮（上传本地图片 MVP1实现，资产库导入占位）
  - 生图控制栏占位：小齿轮 + 生成张数(-/+) + AI生图按钮（MVP1占位，点击创建 generation_tasks 记录并提示"后续版本开放"）
  - 历史记录区占位（底部）
  - 场景详情/道具详情同角色，但无"皮肤"概念
- **依赖**：M1-14
- **验收标准**：点击资产卡片进入详情；修改名称后全局联动（数据库更新 + 分镜列表中的名字同步）；修改描述后保存到数据库并同步刷新左侧列表悬停预览

---

### 第五阶段：顶部工具栏 + 全局联动

#### ✅ M1-16 顶部工具栏骨架
- **做什么**：
  - 左侧：项目名称（双击可编辑）+ 风格标签 + 年代标签 + 模型配置按钮
  - 中间：编辑器按钮 + 画布按钮（MVP2占位）
  - 右侧：生成记录按钮 + 撤销/重做占位 + 导出按钮 + 设置按钮
  - 风格标签点击弹出风格选择面板（左侧网格+右侧示例占位）
  - 年代标签点击弹出年代选择
  - 模型配置按钮弹出模型配置弹窗（骨架：按用途分组Tab，内容占位）
- **依赖**：M1-09
- **验收标准**：双击项目名称可修改；选择风格和年代后保存到 projects 表；模型配置弹窗能打开

#### ✅ M1-17 角色名全局联动
- **做什么**：
  - 修改 `characters.name` 后，遍历当前项目所有 `shots` 记录，在 `description` / `first_frame_prompt` / `last_frame_prompt` / `video_prompt` 中做文本替换（旧名→新名）
  - 关联标签高亮实时更新（基于名称匹配）
  - 数据库层加 `UNIQUE(project_id, name)` 约束
- **依赖**：M1-15
- **验收标准**：在右侧面板修改角色名后，分镜列表中该角色名字同步更新；提示词中的角色名也同步替换；重名时报错提示

#### ✅ M1-18 资产描述编辑联动 + 智能关联高亮
- **做什么**：
  - 右侧面板编辑 `characters.description` → UPDATE 数据库 → 左侧悬停预览同步
  - 分镜提示词编辑时，实时扫描文本中的角色/场景/道具名
  - 匹配成功的名字高亮显示（如浅蓝色背景）
  - 输入不存在的名字时，自动创建新资产并建立关联
- **依赖**：M1-17
- **验收标准**：编辑分镜提示词，已有关联的名字高亮；输入新角色名后按回车，自动创建角色并关联

---

### 第六阶段：批量操作 + 生成记录 + 导出

#### ✅ M1-19 批量操作弹窗 + 缺失判定逻辑
- **做什么**：
  - 各列头"批量生成"按钮点击后弹出面板
  - 面板内容：标题 + 生成次数(- N +) + 全部生成/缺失生成 两个选项
  - **缺失判定逻辑真正实现**：
    - 首帧/尾帧：判定 `first_frame_image_path` / `last_frame_image_path` 是否为空
    - 视频：判定 `video_path` 是否为空
  - 预扫描所有选中项，全部非缺失时"缺失生成"按钮置灰不可点
  - 确认后创建 `generation_tasks` 记录（MVP1只创建记录，不真正调用生成）
- **依赖**：M1-02, M1-12
- **验收标准**：缺失判定逻辑通过单元测试验证（有图=不缺失，无图=缺失）；全部有图时缺失生成按钮置灰；创建的任务记录在 `generation_tasks` 表中

#### ✅ M1-19a 角色/场景/道具批量生成逻辑重构
- **做什么**：
  - **右侧面板批量按钮为入口**：搜索栏旁的"批量生成"按钮点击后，对当前项目**全部资产**（不依赖分镜勾选）弹出批量弹窗，支持"全部生成"和"缺失生成"
  - **列头批量按钮跳转**：出场人物/场景/道具列头的 [批量生成] 按钮点击后，自动切换到右侧面板对应 Tab（characters/scenes/props）并弹出批量弹窗
  - **首帧/尾帧/视频保持分镜勾选逻辑不变**：列头点击仍按选中分镜遍历，需先勾选分镜
  - **shotId 填 null**：角色/场景/道具的批量生成任务，`generation_tasks.shot_id` 为 `null`，`inputParams` 中包含 `characterId` / `sceneId` / `propId`
  - **弹窗统计适配**：资产模式显示"共 N 个角色/场景/道具"，分镜模式显示"已选中 N 个分镜"
- **依赖**：M1-19
- **验收标准**：右侧面板批量按钮无需勾选分镜即可弹出弹窗；列头人物/场景/道具按钮点击后右侧面板切换到对应Tab；首帧/尾帧/视频无分镜勾选时提示"请先勾选分镜"；数据库中资产批量任务 shot_id 为 null

#### ⏳ M1-20 生成记录弹窗
- **做什么**：
  - 点击顶部工具栏"生成记录"按钮弹出窗口
  - 三个 Tab：视频 / 图片 / 其他
  - 表格列：编号 | 所属分镜/素材 | 类型 | 模型 | 渠道 | 创建时间 | 等待时间 | 状态 | 操作
  - 状态显示：✅已完成 / ❌失败 / 🔄排队中 / 🔄生成中
  - 操作：重试（重新下发任务）/ 查看详情
  - 仅显示最近7天的记录
  - **【新增】状态筛选**：按状态快速筛选（全部/排队中/生成中/完成/失败）+ 按类型筛选（视频/图片），记录多了没筛选没法用
  - **【新增】全局任务标注**：所属分镜列，`shot_id` 为 `null` 时显示"项目全局"（角色/场景/道具定妆照），避免空白让用户困惑
  - **【新增】失败原因 hover 显示**：失败状态 hover 直接显示错误摘要（如"API超时"、"余额不足"），减少点进详情的次数
- **依赖**：M1-19a
- **验收标准**：弹窗能显示 `generation_tasks` 表中的记录；状态颜色正确；重试按钮能创建新任务；筛选后列表正确过滤；shotId 为 null 的记录显示"项目全局"

#### ⏳ M1-21 导出功能
- **做什么**：
  - 导出按钮下拉：视频导出 / 场景导出 / 角色导出 / 道具导出
  - **视频导出**：以序号列勾选为依据，弹出文件夹选择，复制 `shots.video_path` 到目标目录
  - **角色/场景/道具导出**：切换到右侧面板对应 Tab，卡片右下角显示勾选框，勾选后导出 `reference_image`
  - 命名规则：`{index}_{projectName}_{timestamp}`
  - **【补全】导出目录选择弹窗**：使用 Electron `dialog.showOpenDialog` 选择目标目录
  - **【补全】批量导出进度提示**：大文件复制时显示进度条/完成提示
- **依赖**：M1-20
- **验收标准**：选择导出目录后，文件成功复制到目标目录，命名符合规则；导出完成后有成功提示

---

### 第七阶段：设置页扩展 + MVP1 收尾

#### ⏳ M1-22 API供应商配置
- **做什么**：
  - 设置页左侧导航：API供应商 & 插件 / 模型路由 & 默认 / 提示词模板 / ...（其他占位）
  - 默认选中第一项，没有空白状态
  - API供应商 & 插件页：供应商列表（名称/标识/Key数量/模型数量）+ 添加/编辑/删除按钮
  - 添加供应商弹窗：名称/baseURL/API Key/模型列表
- **依赖**：无（基于现有 Settings.vue 扩展，可与其它步骤并行）
- **验收标准**：能添加/编辑/删除供应商；添加后 `settings` 表和 `getProviders()` 能读到；模型配置弹窗能读到供应商列表作为渠道选项

#### ⏳ M1-23 版本号管理 + 商业化前置埋点
- **做什么**：
  - `package.json` version 改为 `1.0.0-alpha.1`，遵循 `1.0.0-alpha.x` → `1.0.0-beta.x` → `1.0.0` 的命名规则
  - 代码暴露 `getVersion()` 接口（主进程 + IPC + 前端可用），在"关于"页显示版本号
  - 新增 `license.ts` 模块，MVP1 写死返回 `true`，在**应用启动**、**AI调用**、**导出**三个关键位置预留验证调用点
  - 新增 `telemetry.ts` 模块，MVP1 空实现，预留记录功能使用频率 / AI调用次数 / 错误率的接口
- **依赖**：无
- **验收标准**：`window.api.getVersion()` 返回 `1.0.0-alpha.1`；关于页能看到版本号；license/telemetry 模块存在且编译通过

---

## MVP2（后续实现）

#### M2-01 生图服务接入
- **做什么**：
  - 实现 `generateImage` 函数：接收 `description + stylePrompt + eraPrompt`，调用生图 API
  - 图片保存到 `userData/projects/<id>/assets/images/<category>/`
  - 写入 `character_images` / `scene_images` / `prop_images` / `shot_images` 表
  - 更新对应资产的 `reference_image` 或 `first_frame_image_path`
- **依赖**：M1-03
- **验收标准**：调用后生图 API 返回图片；图片保存到正确目录；数据库有历史记录

#### M2-02 角色/场景/道具定妆照生成
- **做什么**：
  - 右侧面板详情中的生图控制栏接入真实生图服务
  - 模型选择齿轮弹窗显示可用生图模型
  - 生成张数调节（-/+）生效
  - 点击"AI生图"按钮触发生成，进度在生成记录中追踪
  - 历史记录区显示已生成的图，可点击切换选中
- **依赖**：M2-01
- **验收标准**：点击生图后，生成记录弹窗显示进行中的任务；完成后右侧面板显示新图

#### M2-03 首帧/尾帧图生成
- **做什么**：
  - 点击首帧/尾帧列的占位或列头"批量生成"，触发首帧/尾帧生图
  - 使用 `shots.first_frame_prompt` + style + era 拼接提示词
  - 结果保存到 `shot_images` 表，`type = 'first'` 或 `'last'`
  - 右侧面板首帧/尾帧详情接入生图控制栏
- **依赖**：M2-02
- **验收标准**：批量生成首帧后，首帧列显示生成的图片；点击可查看大图

#### M2-04 批量生图全面接入
- **做什么**：
  - 所有批量生成按钮调用真实生图/生视频服务
  - 全部生成模式：覆盖已有结果
  - 缺失生成模式：只生成 `reference_image` / `first_frame_image_path` / `video_path` 为空的项
- **依赖**：M2-03
- **验收标准**：批量生成角色定妆照，只生成还没有图的角色；全部生成则全部重新生成

#### M2-05 视频生成接入
- **做什么**：
  - 实现 `generateVideo` 函数：接收首帧图 + 尾帧图 + videoPrompt + style + era
  - 视频保存到 `assets/videos/`
  - 写入 `shot_videos` 表，`has_new_badge = 1`
  - 更新 `shots.video_path`
- **依赖**：M2-03
- **验收标准**：调用后视频生成 API 返回视频；视频列显示缩略图，带"new"角标

#### M2-06 视频备选素材区
- **做什么**：
  - 右侧面板视频详情：展示窗口 + 备选素材区
  - 备选素材区显示 `shot_videos` 中该 shot 的所有历史记录
  - 每个素材：缩略图 + 左下模型名 + 右下日期 + 悬停显示 ✓选中 / 🗑删除
  - 点击 ✓：更新 `is_selected = 1`，同时更新 `shots.video_path`
  - 点击 🗑：删除单条记录
  - "全部移除"按钮：删除该 shot 下所有 `shot_videos`，清空 `shots.video_path`
  - 点击视频列缩略图或"new"角标，角标消失（`has_new_badge = 0`）
- **依赖**：M2-05
- **验收标准**：生成多个视频后，备选素材区显示多个；切换选中后视频列同步更新

#### M2-07 手动切分 Tab
- **做什么**：
  - AI解析弹窗的第二个 Tab：手动切分
  - 用户可手动输入分镜内容（剧本/首帧提示词/尾帧提示词/视频提示词）
  - 手动添加/删除分镜
  - 手动关联角色/场景/道具
- **依赖**：M1-10
- **验收标准**：Tab切换正常；手动添加的分镜能保存到数据库

#### M2-08 角色皮肤功能
- **做什么**：
  - 角色详情页：右侧"皮肤"缩略图列
  - 支持添加皮肤（上传本地图片）
  - `skin_images` 字段存储皮肤图片路径 JSON 数组
  - 生图时可选择使用哪个皮肤作为参考
- **依赖**：M2-02
- **验收标准**：添加皮肤后，皮肤列显示缩略图；切换皮肤后参考图变化

#### M2-09 资产库导入图片
- **做什么**：
  - 角色/场景/道具详情中的"选择图片"按钮支持"资产库导入"
  - 资产库 = 项目目录下的 `assets/images/` 中已有的图片
  - 弹窗显示资产库缩略图网格，点击选中
- **依赖**：M2-02
- **验收标准**：点击资产库导入能看到已有图片；选中后设置为 `reference_image`

#### M2-10 风格选择面板完整实现
- **做什么**：
  - 风格选择面板左侧：网格卡片带真实预览图（每种风格一张示例图）
  - 右侧：选中风格的多角度人物示例 + 场景示例
  - "+ 自定义风格"按钮：用户上传自己的风格参考图 + 输入风格提示词
  - 自定义风格保存到数据库/配置文件
- **依赖**：M1-16
- **验收标准**：选择自定义风格后，生图时使用自定义风格提示词

#### M2-11 模板系统完整编辑
- **做什么**：
  - 模型配置弹窗中的模板系统：左侧"指令模板"/"我的指令"Tab
  - 右侧编辑区：插入变量栏（输入内容/当前剧本/角色名/场景名等按钮）
  - 指令编辑文本框支持变量占位符（如 `{{角色描述}}`）
  - 保存为自定义模板；保存时校验变量合法性
- **依赖**：M1-07
- **验收标准**：点击变量按钮插入占位符；保存的自定义模板出现在"我的指令"列表

#### M2-12 画布精修模式
- **做什么**：
  - 顶部工具栏"画布"按钮点击进入画布模式
  - 画布中可拖拽调整分镜顺序、合并分镜、拆分分镜
  - 可视化展示分镜之间的转场关系
- **依赖**：M1-11
- **验收标准**：进入画布模式能看到分镜卡片；拖拽可调整顺序

---

## MVP3（更后期）

#### M3-01 TTS 配音接入
- **做什么**：
  - 配音列从占位变为可用
  - 右侧面板配音详情：音色选择（TTS模型）+ 对白文本 + 生成音频按钮
  - 生成结果保存到 `assets/voices/`，更新 `shots.voice_path`
  - 支持试听和替换
- **依赖**：M1-13
- **验收标准**：选择音色后生成音频；配音列显示音频播放器

#### M3-02 ComfyUI 服务器配置
- **做什么**：
  - 设置页新增"ComfyUI服务器"和"ComfyUI工作流"导航项
  - ComfyUI服务器：添加/编辑/删除服务器地址（IP+端口）
  - ComfyUI工作流：上传/管理工作流JSON文件
  - 生图时可选 ComfyUI 作为渠道
- **依赖**：M2-01
- **验收标准**：配置 ComfyUI 服务器后，生图任务可通过 ComfyUI 执行

#### M3-03 配置导出加密
- **做什么**：
  - 模型配置弹窗顶部"导出配置"按钮
  - 导出时支持设置密码加密
  - 导出文件为加密 JSON
  - "导入配置"支持解密导入
- **依赖**：M1-16
- **验收标准**：导出加密配置后，文件无法直接读取；导入时输入密码可还原

#### M3-04 IndexTTS2 配音
- **做什么**：
  - 设置页新增"IndexTTS2配音"导航项
  - 配置 IndexTTS2 服务器和参数
  - 配音模型选择中增加 IndexTTS2 选项
- **依赖**：M3-01
- **验收标准**：可选择 IndexTTS2 生成配音

#### M3-05 火山素材库
- **做什么**：
  - 设置页新增"火山素材库"导航项
  - 配置火山引擎 API Key 和参数
  - 资产库导入时可选"火山素材库"作为来源
  - 搜索火山素材库中的图片/视频资源并导入
- **依赖**：M2-09
- **验收标准**：搜索火山素材库返回结果；导入后成为项目资产

---

## MVP2 任务中心扩展（备忘录补充）

> 以下是对 MVP2/MVP3 中任务中心功能的补充规划，在 MVP1 的 M1-20 基础上扩展

#### M2-13 完整任务中心
- **做什么**：
  - 独立页面或大弹窗，替代 M1-20 的简单记录弹窗
  - 任务队列管理：拖拽排序、暂停/继续/取消、优先级调整
  - 实时预览：生成中的任务显示中间结果缩略图
  - 费用统计：按天/周/月统计API调用次数和费用，**按任务类型拆分**（角色生图¥xx / 视频¥xx / 场景图¥xx），比总量更有决策价值
  - 批量控制：一键重试所有失败任务、一键取消排队中任务
  - 筛选和搜索：按状态/类型/时间范围筛选
  - **按分镜/资产分组折叠**：同一分镜的首帧+尾帧+视频任务，或同一角色的多次生成任务折叠为一组，方便查看"这个分镜做完了吗"
  - **切换渠道重试**：批量重试失败任务时可选择切换到备用渠道，服务商挂了能救命
  - **预算熔断**：设置每日/每月API费用上限，达到后自动暂停队列新任务（不暂停正在跑的），防止账单跑崩
- **依赖**：M2-01
- **验收标准**：任务队列可操作；费用统计按类型拆分显示；预算达到上限后新任务自动暂停

#### M3-06 智能调度
- **做什么**：
  - 并发控制：根据API限流自动调整并发数
  - 智能排期：根据依赖关系自动排序（先角色→再场景→再分镜图→最后视频）
  - 失败自动重试：指数退避，最多3次
  - 预估时间：根据历史数据预估队列完成时间
  - 资源监控：GPU/内存使用情况（ComfyUI场景）
  - **本地缓存命中**：同一分镜/资产+同一参数，之前生成过且本地文件还在，直接返回缓存不再调API，反复调试prompt时省钱
  - **系统级通知**：任务完成或失败时 Electron 托盘气泡 + 音效提醒，不用一直盯着列表
- **依赖**：M2-13
- **验收标准**：依赖任务按正确顺序执行；缓存命中时不再调API；任务完成时有系统通知

---

## 步骤依赖总图

```
MVP1 第一阶段（数据库）
  M1-01 → M1-02 → M1-03

MVP1 第二阶段（服务层）
  M1-04 → M1-05 → M1-06
  M1-02 → M1-07
  M1-05+M1-07 → M1-08

MVP1 第三阶段（总览+解析）
  M1-04 → M1-09 → M1-09b → M1-10

MVP1 第四阶段（剧集结构骨架）
  M1-06 → M1-11 → M1-12 → M1-13
  M1-05 → M1-14 → M1-15

MVP1 第五阶段（工具栏+联动）
  M1-09b → M1-16
  M1-22 → M1-16（模型配置弹窗需要供应商列表作为渠道来源）
  M1-15 → M1-17 → M1-18

MVP1 第六阶段（批量+记录+导出）
  M1-13+M1-02 → M1-19 → M1-19a → M1-20 → M1-21

MVP1 第七阶段（设置+收尾）
  M1-22（独立，可与其它并行）
  M1-23（独立，可与其它并行）

MVP2（图片/视频生成）
  M1-03 → M2-01 → M2-02 → M2-03 → M2-04
  M2-03 → M2-05 → M2-06
  M1-10 → M2-07
  M2-02 → M2-08
  M2-02 → M2-09
  M1-16 → M2-10
  M1-07 → M2-11
  M1-11 → M2-12
  M1-20 → M2-13（任务中心扩展）

MVP3（配音+ComfyUI+智能调度）
  M1-13 → M3-01
  M2-01 → M3-02
  M1-16 → M3-03
  M3-01 → M3-04
  M2-09 → M3-05
  M2-13 → M3-06（智能调度）
```

---

## 状态图例

| 符号 | 含义 |
|------|------|
| ✅ | 已完成 |
| ⏳ | 待开发 |
| 🚧 | 进行中（当前聚焦） |
