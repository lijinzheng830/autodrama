# 工作记忆快照 2026-06-15

## 当前版本
v38（DB 模板版本），代码版本包含当日全部改动

## 当日完成改动

### 数据修复
- normalizeShotData 新增分支处理 `Array.isArray(raw) && raw[0]?.shots`（模板标准输出格式 `[{chapter_title, shots}]` 之前无对应分支，返回空数组导致"分镜为空"）
- v0-01-script-parse.md 新增 `duration_seconds` 字段说明 + 时长与景别挂钩规则 + 总时长 15 秒约束
- ai.ts normalizeShot 返回 + INSERT SQL + bind params 全线补上 duration_seconds
- Editor.vue 新增 getCharColor 函数，角色标签改用 inline style 按名分配颜色
- Editor.css 删除 .char-tag 固定色
- Editor.vue 左侧卡片 duration 显示改为 toFixed(1)
- imageGenerator.ts buildCellFromShot duration 改为 toFixed(1)
- imageGenerator.ts 新增 15s clamp 兜底（总时长超出自动缩放）
- description_en 阈值 50 词降为 30 词

### 遗留问题（已分析未修改）
- description_zh 阈值从 60 字降到 50 字（待执行）
- description_en 阈值从 30 词降到 25 词（待执行）
- 终端 console 输出乱码：start 脚本加 `chcp 65001 >nul &&` 前缀（待执行）
- 15s clamp 保留当前策略，不再追 LLM

### 文档同步
- 代码文档交叉审计报告 → 已移入 archive（过时）
- 综合审核报告、管线优化方案、SD2.0对照优化方案 → 已移入 archive
- AGENTS.md 新增审查流程规范（三层交叉验证 + 验收命令 + 违背后果）
- AGENTS.md 新增 Emoji 禁用规则
- AGENTS.md 新增职能边界：项目经理出方案不定代码

### 桌面文档（SD2.0专用文件夹）中已读但未实现的方案
- ControlNet 线稿锁格 — API 不支持
- 单格局部重绘 — API 不支持
- 参考图权重参数化 — API 不支持
- 多角度参考图 — API 不支持
- 3x3 可视化编辑器 — 未排期

## 关键设计决策
1. 分镜时长为 LLM 输出 + 代码 clamp 双保险（LLM 按景别分配，代码兜底 15s）
2. is_selected 参考图选择：全文4处查询统一用 `ORDER BY is_selected DESC, created_at DESC`，前端点击选中，后端自动尊重
3. extractJSON 的 `[` 提取分支需要从错误位置反向搜索未转义引号（修复中）

## 下一步建议
1. 降 description_zh 到 50 字、description_en 到 25 词
2. package.json start 脚本加 chcp 65001（终端乱码）
3. extractJSON 未转义引号修复
